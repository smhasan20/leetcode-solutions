class Solution {
public:
    int removeElement(vector<int>& nums, int val) {

        int tm=0;
        for(int i=0;i<nums.size();i++)
        {
            if(nums[i]!=val)
            {
                nums[tm]=nums[i];
                tm++;
            }
        } 
        return tm;
        
    }
};
